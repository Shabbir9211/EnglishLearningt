<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnglishTensesActivity"></activity>
        <activity android:name=".EnglishGrammarActivity" />
        <activity android:name=".TodayQustionActivity" />
        <activity android:name=".TodayWordActivity" />
        <activity android:name=".DictionaryActivity" />
        <activity
            android:name=".LearnFreeEnglishActivity"
            android:label="@string/title_activity_learn_free_english"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LearnEnglishActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>